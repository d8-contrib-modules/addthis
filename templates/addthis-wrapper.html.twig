{#
/**
 * @file
 * Default theme implementation to display a node.
 * Available variables:
 * - element: original AddThis wrapper object to reference
 *
 */
#}
<{{ element['#tag'] }} {{ attributes }}>
    {{ element }}
</{{ element['#tag'] }}>

